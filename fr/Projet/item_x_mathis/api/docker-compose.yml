version: '3.8'
services:
  web_container:
    build: .
    container_name: web_container
    volumes:
      - data-volume:/app
    ports:
      - "5000:6000"
    environment:
      - PYTHONPATH=/app
      - FLASK_APP=web_app.py
      - FLASK_ENV=development
      - BACKEND_HOST=web_container
    networks:
      bigdata-main-network:
        ipv4_address: 172.169.20.2

volumes:
  data-volume:
    driver: local
    driver_opts:
      type: none
      device: ../../data
      o: bind

networks:
  bigdata-main-network:
    external:
      name: test2_bigdata-main-network